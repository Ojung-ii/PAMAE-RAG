experiment:
  name: musique_component_refine_cell
  seed: 42

universe:
  max_nodes: 900

pamae:
  retrieval_variant: sample_full_validation_refine_cell_renderer
  renderer: cell_top_rho
  relevance_mode: current
  k: 4
  k_max: 4

