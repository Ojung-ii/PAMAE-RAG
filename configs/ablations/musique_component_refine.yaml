experiment:
  name: musique_component_refine
  seed: 42

universe:
  max_nodes: 900

pamae:
  retrieval_variant: sample_full_validation_refine
  renderer: old
  relevance_mode: current
  k: 4
  k_max: 4

